import styled from 'styled-components';
import media from './media';

const blankspace = styled.blanks`
  margin: 0 auto;
  padding: 0px 0;
  ${media.tablet`padding: 0px 0;`};
`;

export default blankspace;
